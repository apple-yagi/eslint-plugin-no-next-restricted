pre-commit:
  parallel: true
  commands:
    oxfmt:
      glob: "**"
      run: pnpm run format {staged_files}
      stage_fixed: true
    oxlint:
      glob: "*.{js,ts}"
      run: pnpm run lint {staged_files}
      stage_fixed: true
