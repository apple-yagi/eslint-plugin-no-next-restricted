pre-commit:
  parallel: true
  commands:
    oxfmt:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: pnpm run format {staged_files}
      stage_fixed: true
    oxlint:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: pnpm run lint {staged_files}
      stage_fixed: true
